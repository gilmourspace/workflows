name: Swift Coverage Test

on:
    workflow_call:
        secrets:
            DEPLOYSSHSECRET:
                required: true

jobs:

  coverage:
    runs-on: ubuntu-20.04

    steps:
      - uses: ./run-cov-swift.yml
